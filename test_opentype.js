const opentype = require('opentype.js');

async function test() {
    // I need a font file. I'll use a system font or download one?
    // Since I cannot download files easily without a URL, and I don't have one handy.
    // I can try to construct a font or Mock it?
    // Or I can use the tool `view_text_website` to find a url to a font.
    console.log("Setup complete");
}

test();
